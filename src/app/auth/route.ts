export const dynamic = 'force-dynamic';
export const revalidate = 0;

export const metadata = {
  title: 'Auth - Urbex',
  description: 'Autenticación de usuarios en Urbex',
};

export default function route() {
  return {
    layout: true
  };
} 